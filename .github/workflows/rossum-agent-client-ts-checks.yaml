name: Rossum Agent Client TS Checks

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  checks:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci
        working-directory: rossum-agent-client-ts

      - name: Check formatting
        run: npx prettier --check 'src/**/*.ts'
        working-directory: rossum-agent-client-ts

      - name: Type check
        run: npx tsc --noEmit
        working-directory: rossum-agent-client-ts

      - name: Build
        run: npm run build
        working-directory: rossum-agent-client-ts
