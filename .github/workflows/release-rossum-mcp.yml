name: Release rossum-mcp

on:
  push:
    tags:
      - 'rossum-mcp-v*.*.*'
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install

      - name: Extract package version from pyproject.toml
        id: package_version
        run: |
          VERSION=$(grep -E '^version\s*=\s*".*"' rossum-mcp/pyproject.toml | sed -E 's/version\s*=\s*"(.*)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted pyproject.toml version: $VERSION"

      - name: Build package
        run: |
          cd rossum-mcp
          uv build

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: rossum-mcp v${{ steps.package_version.outputs.version }}
          draft: false
          prerelease: ${{ contains(steps.package_version.outputs.version, '-') }}
          files: |
            rossum-mcp/dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_TOKEN }}
          packages-dir: rossum-mcp/dist/
